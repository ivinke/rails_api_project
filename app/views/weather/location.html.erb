<head>
 <title>Simple Map</title>
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no">
    <meta charset="utf-8">
    <style>
      html, body, #map-canvas {
        height: 100%;
        margin: 0px;
        padding: 0px
      }
    </style>
    <script src="https://maps.googleapis.com/maps/api/js?v=3.exp"></script>
    <script>
var map;
function initialize() {
  var mapOptions = {
    zoom: 14,
    center: new google.maps.LatLng("<%= @the_latitude %>","<%= @the_longitude %>")
  };
  map = new google.maps.Map(document.getElementById('map-canvas'),
      mapOptions);
}

google.maps.event.addDomListener(window, 'load', initialize);

    </script>
    </head>

<div class="jumbotron">
  <div class="container">
    <h1>"<%= @b %>"</h1>
  </div>
</div>

<div class="container">
<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Current Temperature</h3>
  </div>
  <div class="panel-body">
    **The current temperature at "<%= @b %>" is "<%= @the_temperature %>" degrees.**
  </div>
</div>

<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Hourly Outlook</h3>
  </div>
  <div class="panel-body">
    **The outlook for the next hour is: "<%= @the_hour_outlook %>"**
  </div>
</div>

<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Daily Outlook</h3>
  </div>
  <div class="panel-body">
    **The outlook for the next day is: "<%= @the_day_outlook %>"**
  </div>
</div>
</div>

 <div id="map-canvas"></div>

</body>
</html>
